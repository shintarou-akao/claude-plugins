# push

現在のブランチをリモートリポジトリにプッシュするプラグイン。

## 含まれるスキル

| スキル名 | コマンド | 説明 |
|---------|---------|------|
| push | `/push` | 現在のブランチを `origin` へプッシュ |

## 使い方

1. `/push` を実行
2. 現在のブランチを自動検出し、`git push -u origin <ブランチ名>` で実行される
